<Page
    x:Class="RFStorage.View.Organisation.KvitteringTilbageLeveringV"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RFStorage.View.Organisation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:RFStorage.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid>
        <RelativePanel >
            <!--<ListView Margin="400,200,0,0" Header="Tilbageleverings Info" Width="auto" Height="Auto" ItemsSource="{Binding TilbageLeveringsOC, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <RelativePanel>
                            <TextBlock Width="Auto" Height="auto" Text="{Binding VareNavn}"/>
                            <TextBlock Width="auto" Height="auto" Text="{Binding VareAntal}"/>
                        </RelativePanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->
            <Button Margin="400,400,0,0" Width="Auto"  Height="Auto" Content="Godkend Tilbagelevering"  Click="ButtonBase_OnClick"></Button>
        </RelativePanel>
    </Grid>
</Page>
