<Page
    x:Class="RFStorage.View.Organisation.OrganisationSøgUdlevering"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RFStorage.View.Organisation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:RFStorage.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModel:OrganisationVM/>
    </Page.DataContext>

    <Grid>
        <AutoSuggestBox x:Name="SuggestBox" Height="100" Width="500" PlaceholderText="Search" TextChanged="SuggestBox_OnTextChanged" />
            <ListView x:Name="OrganList" Width="500" Height="400" ItemsSource="{Binding OrganisationsSingleton.OrganisationOC}"/>

    </Grid>
</Page>
