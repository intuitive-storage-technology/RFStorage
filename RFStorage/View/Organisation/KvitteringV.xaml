<Page
    x:Class="RFStorage.View.Organisation.KvitteringV"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:RFStorage.View.Organisation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:RFStorage.ViewModel"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">


    <Grid>
        <RelativePanel>
            <!--<ListView Margin="400,200,0,0" Header="Udleverings Info" Width="auto" Height="Auto" ItemsSource="{Binding UdleveringsOC, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <RelativePanel>
                            <TextBlock Width="Auto" Height="auto" Text="{Binding VareNavn}"/>
                            <TextBlock Width="auto" Height="auto" Text="{Binding VareAntal}"/>
                        </RelativePanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->
            <Button
                Width="Auto"
                Height="Auto"
                Margin="400,400,0,0"
                Click="ButtonBase_OnClick"
                Content="Godkend Tilbagelevering" />
        </RelativePanel>
    </Grid>
</Page>
